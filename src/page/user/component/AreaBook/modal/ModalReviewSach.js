import { Modal } from 'antd'
import React from 'react'

const ModalReviewSach = ({ data, open = false, onCancel }) => {
    return (
        <Modal open onCancel={onCancel} footer={null} title={title}>

        </Modal>
    )
}

export default ModalReviewSach